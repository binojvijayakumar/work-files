function searchSetImage(srcCtrl, completedCtrl, holdCtrl, closedCtrl, openCtrl, conditionCol, resultCol) {
    $('#' + srcCtrl + ' .FixedTables1>tbody>tr').each(function (index, element) {
        // element == this

    });
}